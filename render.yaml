services:
  - type: web
    name: canvas-scraper
    runtime: node
    rootDir: scraper-service
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    autoDeploy: true