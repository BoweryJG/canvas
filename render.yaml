services:
  - type: web
    name: canvas-scraper
    runtime: docker
    rootDir: scraper-service
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: NODE_ENV
        value: production
    autoDeploy: true